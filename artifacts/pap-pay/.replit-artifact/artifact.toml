kind = "web"
previewPath = "/"
title = "PAP Pay"
version = "1.0.0"
id = "artifacts/pap-pay"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 23740

[services.development]
run = "pnpm --filter @workspace/pap-pay run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/pap-pay", "run", "build" ]
publicDir = "artifacts/pap-pay/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "23740"
BASE_PATH = "/"
